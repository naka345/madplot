language: python
python:
  - "3.8"
# command to install dependencies
install:
  - pip install --upgrade pip
  - pip install pipenv
  - pipenv install --dev
# command to run tests
script:
  - pipenv run pytest
branches:
  only:
    - "master"
    - "/^v\\d\\.\\d\\.\\d/"
    - develop
    - pytest
jobs:
  include:
    - name: "Python 3.8.5 on Xenial Linux"
      python: 3.8
    - name: "Python on macOS"
      os: osx
      osx_image: xcode11.6
      language: shell       # 'language: python' is an error on Travis CI macOS
    - name: "Python 3.8.5 on Windows"
      os: windows
      language: shell
      before_install:
        - choco install python --version 3.8.5
	- python3 --version
	- python --version
	- pip3
        - python -m pip install --upgrade pip
